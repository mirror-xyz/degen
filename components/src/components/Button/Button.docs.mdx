---
title: Button
description: Trigger for actions
---

```tsx
import { Button } from 'degen'
```

```tsx expand=true live=true
<Button>Connect Wallet</Button>
```

## Props

<PropsTable sourceLink={sourceLink} types={types} />

## Variants

```tsx live=true
<Stack direction="horizontal" wrap>
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="tertiary">Tertiary</Button>
  <Button variant="transparent">Transparent</Button>
</Stack>
```

## States

```tsx live=true
<Stack direction="horizontal" wrap>
  <Button loading>Loading</Button>
  <Button disabled>Disabled</Button>
  <Button
    disabled={getState('loading')}
    loading={getState('loading')}
    onClick={() => {
      toggleState('loading')
      setTimeout(() => toggleState('loading'), 1000)
    }}
  >
    Click Me
  </Button>
</Stack>
```

## Sizes

```tsx live=true
<Stack align="center" direction="horizontal" wrap>
  <Button size="large">Connect Wallet</Button>
  <Button size="medium">Connect Wallet</Button>
  <Button size="small">Connect Wallet</Button>
</Stack>
```

## Tones

When `variant` is set to `primary` or `secondary`, you can `tone` to change the button accent color.

```tsx live=true
<Stack space="8">
  <Stack>
    <Text variant="label">Primary</Text>
    <Stack direction="horizontal" wrap>
      <Button tone="accent" variant="primary">
        Accent
      </Button>
      <Button tone="info" variant="primary">
        Blue
      </Button>
      <Button tone="green" variant="primary">
        Green
      </Button>
      <Button tone="red" variant="primary">
        Red
      </Button>
    </Stack>
  </Stack>

  <Stack>
    <Text variant="label">Secondary</Text>
    <Stack direction="horizontal" wrap>
      <Button tone="accent" variant="secondary">
        Accent
      </Button>
      <Button tone="blue" variant="secondary">
        Blue
      </Button>
      <Button tone="green" variant="secondary">
        Green
      </Button>
      <Button tone="red" variant="secondary">
        Red
      </Button>
    </Stack>
  </Stack>
</Stack>
```

## Affixes

Optional `center` prop reserves space for affixes and loading spinner.

```tsx live=true
<Stack space="8">
  <Stack>
    <Text variant="label">Default</Text>
    <Stack direction="horizontal" wrap>
      <Button prefix={<IconPlusSmall />}>Add Entry</Button>
      <Button suffix={<IconPlusSmall />}>Add Entry</Button>
      <Button prefix={<IconPlusSmall />} suffix={<IconPlusSmall />}>
        Add Entry
      </Button>
      <Button loading prefix={<IconPlusSmall />}>
        Add Entry
      </Button>
    </Stack>
  </Stack>

  <Stack>
    <Text variant="label">Center</Text>
    <Stack direction="horizontal" wrap>
      <Button center prefix={<IconPlusSmall />}>
        Add Entry
      </Button>
      <Button center suffix={<IconPlusSmall />}>
        Add Entry
      </Button>
      <Button center prefix={<IconPlusSmall />} suffix={<IconPlusSmall />}>
        Add Entry
      </Button>
      <Button center loading prefix={<IconPlusSmall />}>
        Add Entry
      </Button>
    </Stack>
  </Stack>
</Stack>
```

## Width

```tsx live=true
<Stack>
  <Button width="64">Fixed Width</Button>
  <Button width="full">Full Width</Button>
  <Button width={{ xs: 'full', md: 'max' }}>Responsive Width</Button>
  <Button loading prefix={<IconPlusSmall />} width="full">
    With Prefix & Loading
  </Button>
  <Button center loading prefix={<IconPlusSmall />} width="full">
    Centered With Prefix & Loading
  </Button>
</Stack>
```

## Shapes

```tsx live=true
<Stack direction="horizontal" wrap>
  <Button shape="square">
    <IconPlusSmall />
  </Button>
  <Button shape="circle">
    <IconPlusSmall />
  </Button>
</Stack>
```

## Link

```tsx live=true
<Stack direction="horizontal" wrap>
  <Button as="a" href="https://mirror.xyz" rel="noreferrer" target="_blank">
    Visit Mirror
  </Button>
  <NextLink href="/" passHref>
    <Button as="a">Go to Home</Button>
  </NextLink>
</Stack>
```
