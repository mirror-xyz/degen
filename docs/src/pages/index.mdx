import { Stack } from 'degen/components'

import { Header, Link } from '~/components'
import { createGitHubLink } from '~/utils/github'

import * as styles from '../styles/utils.css'

<Header
  description={
    <>
      Design system for{' '}
      <Link className={styles.link} href="https://mirror.xyz">
        Mirror.xyz
      </Link>{' '}
      built with{' '}
      <Link className={styles.link} href="https://reactjs.org/">
        React
      </Link>{' '}
      and{' '}
      <Link className={styles.link} href="https://vanilla-extract.style/">
        vanilla-extract
      </Link>
      .
    </>
  }
  title="Mirror Design System"
/>

<Stack direction="horizontal">
  <a aria-label="NPM version" href="https://www.npmjs.com/package/degen">
    <img
      alt=""
      src="https://img.shields.io/npm/v/degen.svg?style=for-the-badge&labelColor=323232"
    />
  </a>
  <a aria-label="License" href={createGitHubLink('/LICENSE')}>
    <img
      alt=""
      src="https://img.shields.io/npm/l/degen.svg?style=for-the-badge&labelColor=323232"
    />
  </a>
</Stack>

## Install

```bash
yarn add degen
```

## Set Up

Wrap the root of your app in a [`ThemeProvider`](/components/ThemeProvider), import the external stylesheet, and add [Inter](https://rsms.me/inter/) and [iA Writer Mono](https://github.com/iaolo/iA-Fonts/tree/master/iA%20Writer%20Mono) to your font stack.

```tsx
import { ThemeProvider } from 'degen'
import 'degen/styles'

const App = () => {
  return <ThemeProvider>{children}</ThemeProvider>
}
```

## Use Components

```tsx
import { Button, IconLockClosed, Stack } from 'degen'
```

```tsx live=true expand=true
<Stack align="center">
  <Button
    prefix={<IconLockClosed />}
    tone="blue"
    variant="highlight"
    width={{ xs: 'full', md: 'max' }}
  >
    Connect Wallet
  </Button>
</Stack>
```
