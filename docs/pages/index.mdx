import Layout from 'layouts/docs'

export const meta = {
  title: 'Mirror Design System',
}

The [quick brown](https://mirror.xyz) fox jumped over [the](/components/Button) **lazy** _dogs_.

## Test

Hello World

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
