import Layout from 'layouts/docs'

export const meta = {
  title: 'vars',
  description: 'Themed CSS variables.',
}

```tsx
import { vars } from 'degen'
```

```ts
export const styles = style({
  backgroundColor: vars.colors.background,
  fontSize: vars.fontSizes.small,
  padding: vars.space['4'],
})
```

## Usage

You can use `vars` within [vanilla-extract](https://vanilla-extract.style/) stylesheets (see above) and runtime code. You should only use `vars` if you’re unable to use [`Box`](/components/Box) or [`atoms`](/css/atoms).

```tsx
import { vars } from 'degen'

const FancyButton = () => {
  return (
    <button
      style={{
        backgroundColor: vars.colors.background,
        fontSize: vars.fontSizes.small,
        padding: vars.space['4'],
      }}
    >
      Click Me
    </button>
  )
}
```

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
