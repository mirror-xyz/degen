import Layout from 'layouts/docs'
import { ColorPalette } from 'components'

export const meta = {
  title: 'Colors',
  description: 'Color system used across surfaces.',
}

<ColorPalette />

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
