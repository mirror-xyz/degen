import Layout from 'layouts/docs'
import { IconSearch } from 'components'

export const meta = {
  title: 'Iconography',
  description: 'Collection of icons used across surfaces.',
}

<IconSearch />

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
